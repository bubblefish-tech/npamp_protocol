# Swift build artifacts (SwiftPM scratch/build output). These are regenerated by
# `swift build` (driven by run.sh) and must NEVER be committed. Tracked sources
# — Package.swift, run.sh, and Sources/ — are intentionally NOT ignored.
.lock
build.db
checkouts/
release
release.yaml
repositories/
plugin-tools.yaml
workspace-state.json
x86_64-unknown-linux-gnu/
