// Standalone Gradle settings for the N-PAMP Kotlin/JVM reference library so `gradle -p impl/kotlin`
// builds and publishes it in isolation (it is not part of any umbrella Gradle build).
rootProject.name = "npamp-kotlin"
